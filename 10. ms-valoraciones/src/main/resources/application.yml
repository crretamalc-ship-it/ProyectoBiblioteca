server:
  port: 8090

spring:
  application:
    name: ms-valoraciones
  datasource:
    url: jdbc:mysql://localhost:3307/library_reviews_db?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    root: INFO
